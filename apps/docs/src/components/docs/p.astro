---
import type { HTMLAttributes } from "astro/types";

import { cn } from "@/lib/utils";

type Props = HTMLAttributes<"p">;

const { class: className, ...props } = Astro.props;
---

<p class={cn("leading-relaxed not-first:mt-6", className)} {...props}>
  <slot />
</p>
